---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog", ({ data }) => !data.draft)).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const sessions = (await getCollection("sessions")).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const fmt = (d: Date) => d.toISOString().slice(0, 10);
---

<Layout title="zahid">
  <section class="mb-12">
    <h1 class="text-2xl font-semibold mb-2">hi, i'm zahid</h1>
    <p class="text-[var(--color-muted)]">
      Notes and whiteboard sessions. Sessions are interactive — pan, zoom, click around.
    </p>
  </section>

  <section id="blog" class="mb-12">
    <h2 class="text-lg font-semibold mb-4 text-[var(--color-ink)]">blog</h2>
    <ul class="space-y-2">
      {posts.map((post) => (
        <li class="flex items-baseline gap-3">
          <span class="text-xs text-[var(--color-muted)] font-mono w-24 shrink-0">{fmt(post.data.date)}</span>
          <a href={`/blog/${post.id}`} class="text-[var(--color-ink)] hover:underline">{post.data.title}</a>
        </li>
      ))}
      {posts.length === 0 && <li class="text-[var(--color-muted)] text-sm">no posts yet</li>}
    </ul>
  </section>

  <section id="sessions">
    <h2 class="text-lg font-semibold mb-4 text-[var(--color-ink)]">sessions</h2>
    <ul class="space-y-2">
      {sessions.map((session) => (
        <li class="flex items-baseline gap-3">
          <span class="text-xs text-[var(--color-muted)] font-mono w-24 shrink-0">{fmt(session.data.date)}</span>
          <a href={`/sessions/${session.id}`} class="text-[var(--color-ink)] hover:underline">{session.data.title}</a>
        </li>
      ))}
      {sessions.length === 0 && <li class="text-[var(--color-muted)] text-sm">no sessions yet</li>}
    </ul>
  </section>
</Layout>
